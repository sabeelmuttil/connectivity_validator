# connectivity_validator_example

Demonstrates live (`onConnectivityChanged`) and on-demand (`getConnectivityStatus`) checks.

## Run

```bash
flutter pub get
flutter run          # default device
flutter run -d chrome  # Web — toggle network offline/online to test
```
